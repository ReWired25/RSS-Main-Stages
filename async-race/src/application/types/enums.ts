export enum RaceValues {
  wrapperPadding = 20,
  carWidth = 120,
  marginDefault = '0',
  transitionDefault = '0s',
}

export enum MaxCars {
  carsOnPage = 7,
  winnersOnPage = 10,
}

export enum DriveStatus {
  finished = 200,
  engineBroken = 500,
}
